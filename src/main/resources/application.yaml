spring:
  application:
    name: Lunadir Backend
  datasource:
    url: jdbc:postgresql://localhost:5432/lunadir
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
